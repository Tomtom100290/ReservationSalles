name: Docker CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Récupérer le code
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Se connecter à Docker (facultatif si tu veux juste tester localement)
      # - name: Login to Docker Hub
      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ secrets.DOCKERHUB_USERNAME }}
      #     password: ${{ secrets.DOCKERHUB_TOKEN }}

      # 3️⃣ Construire l’image Docker
      - name: Build Docker image
        run: docker build -t myapp:latest .

      # 4️⃣ (Optionnel) Lancer des tests
      - name: Run tests
        run: docker run --rm myapp:latest pytest  # adapte cette commande à ton projet

      # 5️⃣ (Optionnel) Pousser l’image
      # - name: Push image to Docker Hub
      #   run: |
      #     docker tag myapp:latest ${{ secrets.DOCKERHUB_USERNAME }}/myapp:latest
      #     docker push ${{ secrets.DOCKERHUB_USERNAME }}/myapp:latest
